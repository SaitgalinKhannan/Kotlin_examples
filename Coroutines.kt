package search

fun main() {
    val n = readln().toInt() //Number of people

    var lst = mutableListOf<String>()

    println(lst)



    /*println(
        if (first_str.split(" ").indexOfFirst { it == second_str } != -1) {
            first_str.split(" ").indexOfFirst { it == second_str } + 1
        } else {
            "Not found"
        }
    )*/
}
